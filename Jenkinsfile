pipeline {
    stages {
        stage('Build') {
            steps { 
                echo 'building...'
            }
        }
        stage('Publish') {
            steps { 
                echo 'publishing version ${env.VERSION} ...'
            }
        }
    }
}